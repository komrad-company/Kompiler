# Korelator

## prerequisites

- Cargo
- Rust

## run dev env

> First you need to Create your own configuration file (examples available on `examples`)

You can use the env var `CONFIGURATION_PATH` to set the path for your configuration file (default `./configuration.json`)

Run the command `cargo run`.
